# AI 롤플레이 & 어시스턴트 — 설치와 사용 안내서

LM Studio 가 돌아가는 PC 한 대에서 전부 굴리는 것을 전제로 쓴 문서입니다.
RTX 5080(16GB) + Gemma 4 26B A4B Q4_K_S 기준 설정값이 그대로 들어 있습니다.

이 문서는 **설치하고 앱을 쓰는 사람**을 위한 것입니다. 화면에서 클릭·타이핑만으로 되는 것들을 다룹니다.
소스 코드를 직접 고치거나 새 기능을 붙이고 싶다면 **`DEVELOPER.md`** 를 보세요.

---

## 1. 준비물

| 항목 | 버전 | 받는 곳 |
|---|---|---|
| Node.js | 20 이상 (LTS 권장) | nodejs.org |
| LM Studio | 최신판 | lmstudio.ai |
| 디스크 여유 | 20GB 이상 | 모델 파일 15.5GB + 여유 |

Node 가 깔려 있는지 확인하려면 터미널에서:

```bash
node -v
```

`v20.x` 이상이 나오면 됩니다. 안 나오면 nodejs.org 에서 LTS 를 받아 설치하세요.
설치 후에는 터미널을 새로 열어야 인식됩니다.

---

## 2. 앱 설치

### 2-1. 압축 풀기

`rp-chat.zip` 을 원하는 폴더에 풀어 줍니다. 예: `D:\rp-chat`

경로에 한글이나 공백이 있어도 동작하지만, 문제가 생겼을 때 원인 찾기가 번거로우니
`D:\rp-chat` 처럼 단순한 경로를 권합니다.

### 2-2. 그 폴더에서 터미널 열기

**Windows** — 탐색기로 `rp-chat` 폴더에 들어간 뒤, 주소창을 클릭해 `cmd` 를 치고 Enter.
그 폴더를 가리키는 명령 프롬프트가 열립니다. (PowerShell 을 쓰려면 `powershell` 입력)

**macOS** — 폴더에서 우클릭 → 서비스 → 폴더에서 새로운 터미널 열기

### 2-3. 의존성 설치

```bash
npm install
```

`express` 하나만 받으므로 금방 끝납니다. 처음 한 번만 하면 됩니다.

### 2-4. 실행

```bash
npm start
```

아래처럼 나오면 성공입니다.

```
롤플레잉 채팅 서버: http://127.0.0.1:5173
```

브라우저에서 **http://127.0.0.1:5173** 을 엽니다.

> 이 터미널 창을 닫으면 서버도 꺼집니다. 쓰는 동안은 열어 두세요.
> 끌 때는 터미널에서 `Ctrl + C`.

### 2-5. 다음부터 켜는 법

폴더에서 터미널을 열고 `npm start` 만 하면 됩니다. `npm install` 은 다시 할 필요 없습니다.

매번 터미널을 여는 게 귀찮으면 `rp-chat` 폴더에 `시작.bat` 파일을 만들고 아래 내용을 넣어 두세요.
더블클릭으로 켜집니다.

```bat
@echo off
cd /d "%~dp0"
start "" http://127.0.0.1:5173
npm start
```

### 2-6. 포트가 이미 쓰이고 있다면

`EADDRINUSE` 오류가 나면 다른 포트로 띄웁니다.

```bash
# Windows 명령 프롬프트
set PORT=5174 && npm start

# Windows PowerShell
$env:PORT=5174; npm start

# macOS / Linux
PORT=5174 npm start
```

---

## 3. LM Studio 준비

### 3-1. 모델 받기

LM Studio 의 검색(돋보기) 탭에서 아래를 검색합니다.

```
llmfan46/gemma-4-26B-A4B-it-uncensored-heretic-GGUF
```

양자화 목록에서 **Q4_K_S** 를 고릅니다. 15.5GB 입니다.

> `mmproj` 파일은 이미지 입력용입니다. 글로만 롤플레이할 거면 받지 마세요.

### 3-2. 모델 상세 설정

모델을 로드하기 전 **Show advanced settings** 를 켜고 아래 값으로 맞춥니다.
16GB VRAM 에서 이 모델을 가장 빠르게 돌리는 조합입니다.

| 항목 | 값 | 이유 |
|---|---|---|
| 컨텍스트 길이 | **32768** | 128000 은 KV 캐시만 9GB 넘게 먹습니다. 이 앱은 최근 N턴만 보내므로 32K면 충분합니다 |
| GPU 오프로딩 | **최대치(30)** | 어텐션은 전부 GPU 에 둡니다 |
| Force MoE weights onto CPU | **8로 시작** | VRAM 이 모자란 만큼 전문가 가중치만 RAM 으로 보냅니다 |
| K Cache Quantization | **Q8_0** | 캐시 메모리 절반 |
| V Cache Quantization | **Q8_0** | 위와 같음. Flash Attention 이 켜져 있어야 동작합니다 |
| Flash Attention | **켜기** | |
| Offload KV Cache to GPU | **켜기** | |
| Max Concurrent Predictions | **1** | 4면 캐시를 4슬롯으로 쪼개 3슬롯이 낭비됩니다 |
| 전문가 수 | 8 (기본값) | 건드리지 마세요 |
| 평가 배치 크기 / Physical Batch | 기본값 | |

**Force MoE weights onto CPU 값 정하는 법**

8로 두고 로드해 봅니다. 화면 위쪽 *Estimated Memory Usage* 의 GPU 수치가 **15GB 근처**면 적당합니다.
여유가 많이 남으면 7, 6 으로 줄여 보세요. 숫자가 작을수록 GPU 를 많이 쓰고 빨라집니다.
로드가 실패하거나 GPU 수치가 15.5GB 를 넘으면 다시 한 칸 올리세요. 데스크톱 화면용으로 1GB 정도는 남겨 둬야 합니다.

> **왜 GPU 오프로딩을 낮추지 않나요**
> GPU 오프로딩을 낮추면 그 레이어의 어텐션까지 CPU 로 갑니다. 어텐션은 GPU 이득이 가장 큰 부분입니다.
> 반면 MoE 오프로드는 어텐션을 GPU 에 남기고 전문가 가중치만 내립니다. 레이어당 128개 전문가 중
> 9개만 활성화되므로 CPU 가 감당할 양이 적습니다. 같은 VRAM 을 아끼면서 훨씬 빠릅니다.

설정 아래쪽 **Remember settings for …** 체크를 켜 두면 다음부터 이 값이 유지됩니다.

### 3-3. 서버 켜기

왼쪽의 **Developer**(개발자) 탭으로 가서 **Start Server** 를 누릅니다.
포트가 **1234** 인지 확인하세요.

같은 PC 에서 앱을 돌리므로 *Serve on Local Network* 는 꺼 두어도 됩니다.

브라우저에서 http://localhost:1234/v1/models 를 열어 JSON 이 나오면 정상입니다.

---

## 4. 앱 첫 설정

### 4-1. 엔진 연결

왼쪽 아래 **설정** 을 누릅니다.

1. 사용할 엔진: **LM Studio (로컬)**
2. 서버 주소: `http://localhost:1234/v1` (기본값 그대로)
3. **불러오기** 클릭 → 모델 목록이 뜹니다
4. 모델 칸을 눌러 `gemma-4-26b-a4b-it-uncensored-heretic` 선택
5. **저장**

입력창 아래에 `LM Studio · gemma-4-26b-…` 라고 뜨면 연결된 것입니다.

### 4-2. 샘플링 값

기본값이 이미 Gemma 4 권장치입니다. 아래만 확인하세요.

| 항목 | 값 |
|---|---|
| 온도 | 1.0 |
| top_k | 64 |
| 응답 최대 길이 | 2048 |
| 컨텍스트 길이 (엔진 칸) | LM Studio 의 Context Length 와 **같은 값** |
| 최대 메시지 수 | 40 (그대로 두어도 됩니다) |

앱은 대화를 메시지 개수가 아니라 **토큰으로 어림해서** 자릅니다.
컨텍스트 길이에서 시스템 프롬프트(설정·기억)와 답변 여유(응답 최대 길이)를 뺀 만큼, 최근 메시지부터 채워 보냅니다.
입력창 아래 게이지가 지금 얼마나 찼는지, 옛 메시지가 몇 개 잘렸는지 보여 줍니다. 마우스를 올리면 자세한 내역이 나옵니다.

토큰 수는 글자 종류로 어림한 값이라 처음엔 조금 틀릴 수 있습니다. 엔진이 실제 토큰 수를 알려 주면
(LM Studio·OpenAI·Anthropic·Gemini 모두 알려 줍니다) 그 비율을 기억해 다음부터는 맞춰 어림합니다.
*최대 메시지 수* 는 토큰 여유가 있어도 이 개수를 넘겨 보내지 않는 상한입니다. API 요금을 아끼고 싶을 때 줄이세요.

### 4-3. 내 페르소나

**내 페르소나** 버튼을 누르고 이름과 소개를 넣습니다.
여기 넣은 이름이 프롬프트의 `{{user}}` 자리에 들어갑니다.
여러 개를 만들어 두고 **사용** 버튼으로 갈아탈 수 있습니다.

**사용** 은 앞으로 새로 여는 대화의 기본값입니다. 이미 진행 중인 대화는 상단의 **나: 이름** 선택기로
그 대화만 따로 바꿉니다. 바꾼 뒤 다음 답변부터 반영되고, 이미 쓰인 첫 대사 속 이름은 그대로 남습니다.

---

## 5. 캐릭터 만들기

왼쪽 **캐릭터 → 만들기**.

| 칸 | 쓰임 | 비중 |
|---|---|---|
| 아이콘 | 목록에 뜨는 이모지 한 글자 | |
| 이름 | `{{char}}` 에 들어갑니다 | |
| 태그 | 목록 정리용 | |
| 한 줄 소개 | 캐릭터가 누구인지 | 중 |
| 성격 | 연기의 뼈대 | **상** |
| 말투 | 어미, 호흡, 버릇 | **상** |
| 배경과 상황 | 첫 장면의 무대 | 중 |
| 첫 대사 | 대화를 열 때 자동으로 놓이는 메시지 | 중 |
| 대화 예시 | 말투를 흉내 낼 본보기. 2~4턴이면 충분 | **상** |
| 추가 설정 | 세계관 규칙, 관계도, 금기 | |

성격·말투를 추상적으로("매력적이다") 쓰면 효과가 거의 없습니다.
"놀릴 때 말끝을 길게 끈다", "사과할 때 꼭 딴 얘기부터 꺼낸다" 처럼 **관찰 가능한 행동**으로 쓰세요.

대화 예시가 가장 강하게 먹힙니다. 원하는 문체로 두세 턴을 직접 써 넣으면 그 톤을 따라갑니다.

### 들어 있는 기본 캐릭터

캐릭터 머리의 **기본 캐릭터** 를 누르면 아래 여섯이 들어옵니다.
이미 같은 이름이 있으면 건너뛰므로, 여러 번 눌러도 중복되지 않습니다.

| 캐릭터 | 관계 | 어울리는 장면 |
|---|---|---|
| 🌙 유하린 | 같은 과 동기, 21세 | 학원물. 무심한 척 챙기는 관계 |
| 🎧 서다인 | 동아리 새내기 부원, 20세 | 발랄한 후배와의 일상 |
| 🍬 오소율 | 친한 동생, 20세 | 애교 많고 스킨십에 거리낌 없는 사이 |
| 🧸 한소이 | 소꿉친구, 20세 | 성인 모드. 거리낌 없던 사이의 변화 |
| 🏠 강여름 | 룸메이트, 21세 | 성인 모드. 한집에 사는 긴장감 |
| 🌧️ 윤소원 | 2년 만의 옛 연인, 22세 | 성인 모드. 거리를 재는 긴장 |

아래 셋은 성인 모드와 함께 쓰라고 만든 캐릭터이지만, 설정 자체에는 노골적인 내용이 없습니다.
수위는 어떤 모드를 고르느냐로 정해집니다. 일반 모드로 쓰면 그냥 연애물이 됩니다.

기본 캐릭터은 모두 20대 성인입니다. 어떤 캐릭터가든 성인 모드 모드와 짝지어 쓸 수 있는 구조라서,
내장 캐릭터는 전부 성인으로만 넣었습니다. 직접 만드는 캐릭터도 같은 기준을 지켜 주세요.

지우거나 고쳐도 됩니다. 원본이 필요하면 **기본 캐릭터** 을 다시 누르면 됩니다.
다만 같은 이름이 이미 있으면 건너뛰기 때문에, 예전 버전으로 들어온 캐릭터를 새 설정으로 바꾸려면
그 캐릭터를 먼저 지우고 **기본 캐릭터** 을 누르세요.


### 자리표시자

| 쓰는 법 | 바뀌는 값 |
|---|---|
| `{{char}}` | 캐릭터 이름 |
| `{{user}}` | 내 페르소나 이름 |

조사는 받침에 맞춰 자동으로 고쳐집니다. `{{char}}가` 라고 써 두면 "유하린이", "루나가" 로 각각 나옵니다.

### 캐릭터 다루기

- **클릭** — 그 캐릭터와 새 대화 시작
- **우클릭** 또는 **Shift+클릭** — 편집 창 열기

---

## 6. 다른 AI에게 캐릭터 만들어 달라고 하기

캐릭터 칸을 채우기 귀찮을 때, 아래를 통째로 복사해 다른 AI(또는 LM Studio의 로컬 모델)에 붙여 넣으세요.
**■ 내가 원하는 것** 세 줄만 내 아이디어로 바꾸면 됩니다. 나머지는 그대로 둡니다 — 뒤에 붙은
예시가 형식을 잡아 주는 역할이라, 지우면 오히려 결과가 들쭉날쭉해집니다. 결과를 앱의 각 칸에
그대로 옮겨 담으면 끝입니다.

````text
아래 조건으로 롤플레잉 채팅용 캐릭터 시트를 만들어 줘.

■ 내가 원하는 것
- 분위기/장르: (예: 현대 일상, 잔잔한 대화 위주)
- 나와의 관계: (예: 오래 다닌 단골 서점 주인)
- 꼭 넣고 싶은 것: (예: 말수가 적고, 책 추천으로 대화를 대신하는 사람)

■ 출력 규칙
1. 아래 예시와 정확히 같은 형식으로 써. 항목 제목([이름], [아이콘] 등)을 그대로 달고,
   설명이나 서론은 붙이지 마.
2. 형용사로 때우지 말고 관찰 가능한 행동으로 써.
   나쁜 예: "다정하다", "매력적이다"
   좋은 예: "걱정된다는 말 대신 먹을 것을 내민다", "칭찬할 때 꼭 딴 데를 본다"
3. 캐릭터 이름은 {{char}}, 상대역은 {{user}} 로 표기해. 실제 이름을 직접 쓰지 마.
   (한국어 조사는 앱이 알아서 고치니 {{char}}가, {{char}}는 처럼 자연스럽게 붙여 써.)
4. 등장인물은 모두 성인이야. 나이를 한 줄 소개에 명시해.
5. 행동과 묘사는 *별표 안에*, 대사는 "큰따옴표 안에" 써.
6. 한국어로만 써.
7. 아래는 형식을 보여 주는 예시일 뿐이야. 내용은 절대 베끼지 말고,
   위 ■ 내가 원하는 것 에 맞춰 완전히 새로운 캐릭터로 써.

■ 예시 (형식만 참고, 내용은 새로 쓸 것)

[이름]
김하온

[아이콘]
📚

[태그]
일상, 서점, 잔잔함

[한 줄 소개]
3년째 다니는 단골 서점의 주인, 24세. 말없이 책부터 골라 건넨다.

[성격]
말수가 적고 먼저 다가오지 않는다. 대신 상대가 무슨 책을 뒤적이는지, 요즘 뭘 읽는지로 그 사람의 기분을 짐작한다.
위로할 때 말로 하지 않고 책 한 권을 말없이 건네는 식이다. 정작 자기 얘기가 나오면 책 정리를 핑계로 자리를 피한다.

[말투]
낮고 느린 존댓말. 문장을 끝까지 맺지 않고 흐릴 때가 많다. 편해진 상대에게는 가끔 반말이 섞여 나온다.

[배경과 상황]
비 오는 평일 저녁, 손님이 끊긴 서점. {{char}}가 계산대에서 책을 정리하다 문에 달린 종소리에 고개를 든다.
우산을 접으며 들어서는 {{user}}를 발견한다.

[첫 대사]
*책을 정리하던 손을 멈추고 고개를 든다.* "비 많이 오죠." *잠깐 서가 쪽을 본다.* "오늘은 뭐 찾으세요, 아니면 그냥 피하러 오신 거예요?"

[대화 예시]
{{user}}: 그냥 비 피하러 왔어요.
{{char}}: *옅게 웃는다.* "그런 손님이 제일 편해요." *창가 의자를 가리킨다.* "저기 앉아 계세요. 그칠 때까지."
{{user}}: 여기 자주 오는데 사장님은 늘 조용하시네요.
{{char}}: *잠깐 멈칫한다.* "...말이 많으면 책 읽는 소리가 안 들리잖아요." *다시 책 쪽으로 시선을 돌린다.*

[추가 설정]
서점 안쪽 방에 아무에게도 안 보여주는 서가가 있다. 누가 그 얘기를 꺼내면 못 들은 척한다.
````

### 왜 예시를 프롬프트 안에 넣었는가

지시문만 주면 AI마다 분량과 어조가 들쭉날쭉합니다. "3~4문장" 같은 지시보다,
실제로 3~4문장짜리 문단을 하나 보여 주는 쪽이 훨씬 정확하게 통합니다. 특히 로컬 모델일수록
말로 된 지시보다 눈앞의 예시를 그대로 따라 쓰는 경향이 강합니다.

예시의 내용(서점 주인 이야기)은 절대 베끼지 말라고 못박아 뒀습니다. 이게 없으면 종종
"김하온이랑 비슷한 서점 캐릭터"를 그대로 재활용해서 돌려주는 모델이 있습니다.

### 성인용 캐릭터를 부탁할 때

위 프롬프트의 **■ 출력 규칙** 4번을 아래로 바꿔 쓰세요. 예시는 그대로 둬도 됩니다 — 예시 자체는
성인 소재가 아니라 형식을 보여줄 뿐이라, 수위 지시만 바뀌면 그 결에 맞는 캐릭터가 나옵니다.

````text
4. 등장인물은 모두 성인이야. 20세에서 23세 사이로 잡고, 나이를 한 줄 소개에 명시해.
   이 시트는 성인 대화용이지만, 시트 자체에는 노골적인 성적 묘사를 넣지 마.
   관계의 긴장, 거리감, 서로에게 끌리는 이유를 행동으로 보여주는 데 집중해.
   수위는 앱에서 대화 모드로 정할 거야.
````

수위를 시트에 박아 넣으면 일반 대화로 쓸 때 못 쓰게 됩니다. **시트는 관계를, 모드는 수위를** 맡는 구조로
가야 캐릭터 하나를 양쪽에 다 쓸 수 있습니다.

### 결과를 미리보기로 확인하기

받은 답을 캐릭터 창의 같은 이름 칸에 그대로 옮겨 담고 저장하세요. 그 캐릭터를 클릭하면 뜨는
**모드를 고르는 창**에 이렇게 나오면 잘 옮긴 것입니다.

```
📚 김하온                                    [일상, 서점, 잔잔함]
3년째 다니는 단골 서점의 주인, 24세. 말없이 책부터 골라 건넨다.
─────────────────────────────────────────────
성격   말수가 적고 먼저 다가오지 않는다. 대신 상대가 무슨 책을...
말투   낮고 느린 존댓말. 문장을 끝까지 맺지 않고 흐릴 때가 많다...
배경   비 오는 평일 저녁, 손님이 끊긴 서점. 김하온이 계산대에서 책을 정리하다...
       우산을 접으며 들어서는 도윤을 발견한다.
설정   서점 안쪽 방에 아무에게도 안 보여주는 서가가 있다...
```

`{{char}}` `{{user}}` 자리에는 실제 캐릭터 이름과 내 페르소나 이름이 채워져 들어갑니다.
위 예시는 페르소나가 "도윤"일 때의 모습이고, 조사도 받침에 맞춰 자동으로 고쳐집니다.

**첫 대사와 대화 예시는 이 미리보기에 나오지 않습니다.** 실제 대화가 시작될 때만 쓰이기 때문입니다.
그러니 미리보기에 무언가 비어 보여도, 저장할 때 [한 줄 소개] [성격] [말투] [배경과 상황] [추가 설정]
다섯 칸만 제대로 채워져 있으면 됩니다. 태그가 없으면 대괄호 배지도 빠집니다.

### 결과를 옮길 때 확인할 것

- `{{char}}` 를 실제 이름으로 바꿔 써 온 경우가 많습니다. 그대로 두면 내 페르소나 이름이 반영되지 않으니 되돌려 놓으세요
- 대화 예시가 너무 길면 잘라내세요. 2~4턴이면 충분하고, 길수록 매 턴 보내는 토큰만 늘어납니다
- 성격이 형용사 나열이면 다시 요청하세요. "행동으로 다시 써 줘" 한마디면 대개 고쳐 옵니다

---

## 7. 대화하기

### 입력창 아래 빠른 전환

엔진 선택기가 입력창 아래에 있습니다. 설정을 열지 않고 여기서 바로 LM Studio ↔ API 를 오갈 수 있습니다.
바꾸면 **다음 응답부터** 적용되고, 대화 기록은 엔진과 무관하게 하나로 쌓입니다.

옆의 모델 이름을 누르면 설정이 열립니다. 모델이 지정되지 않았으면 빨갛게 표시됩니다.

지금 대화가 성인 모드를 쓰고 있으면 외부 API 엔진은 목록에서 회색으로 잠깁니다.
로컬 주소로 등록한 커스텀 엔진은 고를 수 있습니다. 개발자 설정에서 클라우드 허용을 켰다면 잠기지 않습니다 (9절 참고).

### 조작

| 동작 | 방법 |
|---|---|
| 전송 | Enter (한글 조합 중에는 안 나갑니다) |
| 줄바꿈 | Shift + Enter |
| 다시 생성 | 입력창 옆 **재전송** — 마지막 답변의 다른 버전을 씁니다. 이전 버전은 답변 아래 **‹ ›** 로 넘겨봅니다. 마지막 장에서 › 를 눌러도 새 버전을 씁니다 |
| 이어쓰기 | 입력창 옆 **이어쓰기** — 잘린 답변 끝에 이어서 씁니다 |
| 대신 쓰기 | 입력창 아래 **✍ 대신 쓰기** — 내 차례 초안을 입력창에 써 줍니다. 입력창에 적어 둔 글은 방향으로 씁니다. 보내기 전에 고칠 수 있습니다 |
| 기억·작가 노트 | 대화 상단 **기억·노트** — AI 가 답변 4개마다 뒤에서 모으는 *기억해 둔 사실*, 옛 대화 요약, 매 턴 몰래 붙는 진행 지시 |
| 여러 인물 | 대화 상단 **등장인물** — 다른 캐릭터를 같은 장면에 부릅니다 |
| 생성 중단 | **중지** — 그때까지 쓴 답변은 남습니다 |
| 메시지 수정 | 메시지에 마우스를 올리면 나오는 **수정** |
| 메시지 삭제 | 같은 자리의 **삭제** |

**AI 응답을 직접 고치는 게 가장 강력한 조정 수단입니다.** 고친 내용이 그대로 다음 맥락에 들어가므로,
어투가 어긋나기 시작했을 때 한 번 손봐 주면 이후 응답이 그 방향으로 따라옵니다.

### 표기법

화면은 두 가지를 모두 알아봅니다.

```
*난간에 기댄다.* "왔구나."          ← 롤플레이 틀
(빗줄기가 굵어진다.)
유하린: "이 시간에 여긴 왜 왔어."    ← 소설가 모드 틀
```

별표와 괄호는 묘사체로, 줄 맨 앞의 `이름:` 은 화자 라벨로 렌더링됩니다.

---

## 8. 대화 모드 바꾸기

설정 → **대화 모드** 에서 고릅니다. 기본으로 여덟 개가 들어 있고, 일반 넷과 성인 넷으로 묶여 있습니다.

| 모드 | 표기 | 호흡 | 수위 |
|---|---|---|---|
| 롤플레이 | `*별표*` 행동, `"따옴표"` 대사 | 한 번에 2~5문장 | 일반 |
| 소설 모드 | `(괄호)` 묘사, `이름: "대사"` | 길고 촘촘한 서술, 여러 인물 | 일반 |
| 연출 모드 | `(괄호)` 묘사, `이름: "대사"` | 내가 상황만 지시, AI 가 전부 서술 | 일반 |
| 메신저 모드 | 묘사 없이 문자만. 한 줄이 문자 하나, 말풍선으로 표시 | 한 번에 문자 1~4개 | 일반 |
| 성인 롤플레이 | `*별표*` 행동, `"따옴표"` 대사 | 짧은 호흡의 주고받기 | 성인 🔒 |
| 성인 소설 모드 | `(괄호)` 묘사, `이름: "대사"` | 길고 촘촘한 서술, 여러 인물 | 성인 🔒 |
| 성인 연출 모드 | 〃 | 〃 | 성인 🔒 |
| 성인 메신저 모드 | 메신저 모드와 같음 | 〃 | 성인 🔒 |

가로가 표기법과 호흡, 세로가 수위입니다. 같은 관계를 여덟 가지 결로 쓸 수 있습니다.
롤플레이 계열은 티키타카에, 소설·연출 계열은 한 장면을 통째로 그리는 데 맞습니다.

목록에 뜨는 순서는 항상 일반 셋(롤플레이·소설 모드·연출 모드)이 먼저, 성인 셋이 뒤에 옵니다.
예전 버전을 쓰다 업그레이드했다면 처음 실행할 때 한 번 이 순서로 재정렬되고, 직접 만든 모드는
그 뒤에 원래 있던 자리를 지킵니다.

### 연출 모드 — 나는 감독, AI 가 전부 연기

소설 모드와 뭐가 다르냐면, **나는 대사를 직접 치지 않습니다. 감독처럼 장면만 지시합니다.**

| | 소설 모드 | 연출 모드 |
|---|---|---|
| 내 위치 | 등장인물. 내 대사는 내가 씀 | 감독. 지시만 내림 |
| 내가 보내는 글 | 내 캐릭터의 대사와 행동 | "이렇게 전개하라" 는 지시 |
| AI 가 쓰는 것 | 상대 캐릭터 몫 | 주인공은 물론, 필요하면 내 페르소나 몫까지 |

```
비가 그치고, 서린이 처음으로 먼저 말을 건다
```

이렇게 한 줄만 던지면 그 장면을 통째로 써 내려갑니다. 주인공의 대사와 속마음까지 전부요.

내 페르소나가 장면에 필요하면 — 주인공이 그에게 말을 걸거나 함께 있는 장면이 필요하면 —
**그 몫도 AI 가 대신 씁니다.** 나는 이야기 속 인물이 아니라 이야기를 지시하는 사람이기 때문입니다.
빈자리로 남겨 두고 내 대답을 기다리지 않습니다.

내 대사를 직접 치고 싶어지면 대화 상단에서 소설 모드로 바꾸면 됩니다. 기록은 그대로 이어집니다.

### 소설 모드의 여러 인물

소설 계열 두 모드는 **주변 인물까지 함께 등장시켜 서로 대화하게** 합니다.
상황만 던져 주면 그 장면에 어울리는 인물을 필요한 만큼 만들어 넣습니다.

```
카페, 마감 시간 직전. 옆 테이블에서 말다툼이 벌어진다.
```

이렇게만 적어도 주인공 캐릭터 외에 옆 테이블 손님, 점원 같은 인물이 등장해 각자 말합니다.
인물마다 말투를 다르게 쓰고, 의견이 갈리게 하고, 주인공이 중심에서 밀려나지 않게 하라는 지시가 들어 있습니다.

내 페르소나의 대사와 행동만은 대신 쓰지 않습니다. 내가 말할 자리는 비워 두고 넘깁니다.

특정 조연을 고정해 두고 싶다면 캐릭터의 **추가 설정** 칸에 적어 두세요.
"단짝 친구 서연 — 말이 빠르고 참견이 많다" 처럼 한 줄이면 충분합니다.

### 원래대로 되돌리기

시스템 프롬프트 아래의 선택기에서 기본 모드을 고르고 **가져오기** 를 누르면, 지금 편집 중인 모드의 내용이
그 원본으로 덮어써집니다. 내장 모드를 편집 중이면 같은 모드의 원본이 자동으로 잡힙니다.

네 가지 원본 중 아무거나 고를 수 있으므로, 직접 만든 모드에 소설 모드 원본을 부어 넣고
고쳐 쓰는 식으로도 쓸 수 있습니다. 덮어쓰기 전에 확인을 한 번 묻고, **저장** 을 눌러야 확정됩니다.

**새로 만들기** 로 직접 쓴 프롬프트를 추가할 수 있습니다. 아래 자리표시자가 채워집니다.

| 자리표시자 | 들어가는 값 |
|---|---|
| `{{char}}` `{{user}}` | 캐릭터 이름 / 페르소나 이름 |
| `{{description}}` | 캐릭터의 한 줄 소개 |
| `{{personality}}` `{{speech}}` `{{scenario}}` | 캐릭터의 성격 / 말투 / 배경 |
| `{{persona}}` | 페르소나 소개 |

비어 있는 항목은 라벨이 붙은 줄까지 통째로 빠집니다.
`{{char}}` 가 한 번도 없는 모드에는 캐릭터 블록이 맨 뒤에 자동으로 붙으므로,
자리표시자를 모르는 프롬프트를 그대로 붙여 넣어도 캐릭터 정보가 반영됩니다.

바꾼 모드는 **다음 응답부터** 적용됩니다. 이미 쌓인 대화는 그대로 남습니다.

---

## 9. 성인 모드

### 어떻게 나뉘는가

등급은 **대화 모드**에 붙습니다. 설정에서 모드를 편집할 때 *성인 전용 모드* 을 체크하면 그 모드는 성인용이 됩니다.
기본으로 **성인 롤플레이** 와 **성인 소설 모드** 두 가지가 들어 있습니다.

| 모드 | 결 |
|---|---|
| 성인 롤플레이 | `*별표*` 표기. 주고받는 호흡이 빠르고, 상대의 반응을 보며 나아갑니다 |
| 성인 소설 모드 | `(괄호)` 표기. 한 번에 한 장면을 통째로 그립니다 |

대화마다 어떤 모드를 쓰는지 따로 기억합니다. 대화 상단의 선택기에서 언제든 바꿀 수 있고,
바꾸면 **다음 응답부터** 적용됩니다. 쌓인 대화는 그대로 남습니다.

즉 같은 캐릭터로 일반 대화와 성인 대화를 따로 만들어 두고 오갈 수 있습니다.

### 목록에서 구분

- 성인 모드를 쓰는 대화에는 목록에 **19** 배지가 붙습니다
- 대화 목록 위의 **성인 숨기기** 을 누르면 목록에서 감춰집니다. 화면을 남에게 보일 때 쓰세요
- 이 설정은 브라우저에만 저장됩니다. 서버 데이터는 건드리지 않습니다

### 로컬 전용 잠금

성인 모드로 표시된 대화는 **LM Studio 로만 나갑니다.** 엔진이 OpenAI · Anthropic · Gemini 로 되어 있으면
요청을 보내지 않고 아래처럼 막습니다.

```
'성인 롤플레이' 모드는 로컬 엔진 전용입니다.
'Google Gemini' 은 로컬 주소가 아니라 보내지 않았습니다.
```

외부 API 는 대부분 각사 정책에 걸려 거부되므로, 실수로 내보내 계정에 흔적을 남기거나
요금을 쓰는 일을 기본으로 막아 둔 것입니다. 모드 선택기에서 자물쇠(🔒)가 붙은 항목이 성인 틀입니다.

### 클라우드 엔진 허용 (숨은 옵션)

클라우드 엔진으로도 성인 모드를 쓰고 싶다면 잠금을 풀 수 있습니다. 실수로 켜지 않도록 평소에는 숨겨 두었습니다.

1. 설정 → **개발자 설정** → *성인 모드 엔진 제한* 에서 **클라우드 엔진 제한 풀기…** 를 누릅니다
2. 경고 창을 읽고 *이해했습니다* 에 체크한 뒤 **제한 풀기** 를 누릅니다
3. 나타난 *클라우드(외부 API) 엔진에서도 성인 모드 사용* 이 체크된 상태로 **저장** 합니다

경고 창에 적힌 내용은 이렇습니다.

- 대화 전체(시스템 프롬프트, 캐릭터·페르소나 설정, 메시지)가 선택한 엔진의 회사 서버로 전송되고, 회사 정책에 따라 저장·검토될 수 있습니다
- 대부분의 클라우드 서비스는 이용 약관으로 성적인 콘텐츠 생성을 제한합니다. 요청이 거부되거나, 반복되면 API 키·계정이 정지될 수 있습니다
- 거부된 요청에도 요금이 나갈 수 있습니다
- 답변뿐 아니라 요약·기억 정리, 대신 쓰기, 장면 그리기의 태그 변환, 성인 페르소나 문장 만들기도 클라우드로 나갑니다

켠 뒤에도 성인 모드의 절대 규칙(등장인물은 모두 성인, 실존 인물 금지)과 그림의 고정 차단 목록은 그대로입니다.
다시 잠그려면 같은 칸의 체크를 풀고 저장하세요. 체크 칸도 다시 숨겨집니다.
이 설정은 백업 파일로 옮겨지지 않습니다 — 새로 불러온 곳에서는 경고를 다시 확인해야 켜집니다.

### 성인 모드에 들어 있는 내용

두 모드 모두 같은 뼈대를 씁니다. 표기법과 호흡만 다릅니다.

완곡한 표현으로 돌리지 말고 필요한 만큼 직접적으로 쓰라는 지시, 훈계나 경고문을 붙이지 말라는 지시,
그리고 장면을 서두르지 말고 분위기부터 쌓으라는 연출 지시가 들어 있습니다.
캐릭터가 무조건 순응하지 말고 성격에 맞으면 거절하거나 주도권을 가져가라는 항목도 있습니다.
이게 있고 없고가 장면의 질을 크게 가릅니다.

아래 세 줄은 **사용자 지시보다 우선한다**고 못 박아 두었습니다. 지우지 마세요.

- 등장인물은 전원 성인. 미성년으로 읽힐 수 있는 인물은 성적 맥락에 등장시키지 않음
- 실존 인물을 성적 맥락에 등장시키지 않음
- 허구이며, 현실에서 사람을 해치는 방법은 안내하지 않음

로컬 모델은 이런 제약을 100% 지키지는 않습니다. 프롬프트로 방향을 강하게 잡아 줄 뿐입니다.

### 직접 만들기

두 성인 모드 중 가까운 쪽을 골라 **새로 만들기** 로 사본을 만든 뒤 내용을 고치는 방식을 권합니다.
*성인 전용 모드* 체크를 반드시 켜 두어야 로컬 잠금이 걸립니다.

---

## 10. 어시스턴트 모드

롤플레이가 아닌 일반 AI 채팅입니다. 사이드바 맨 위 **어시스턴트** 탭으로 넘어갑니다.

캐릭터도 페르소나도 쓰지 않습니다. 설정에 있는 어시스턴트용 시스템 프롬프트와 대화 내용만 보냅니다.
엔진은 롤플레이와 같은 것을 씁니다 — LM Studio 든 API 든 설정에서 고른 엔진이 그대로 적용됩니다.

| | 롤플레이 | 어시스턴트 |
|---|---|---|
| 캐릭터·페르소나 | 씁니다 | 안 씁니다 |
| 시스템 프롬프트 | 대화 모드 | 설정의 어시스턴트 프롬프트 하나 |
| 화면 표기 | `*별표*` 묘사, 화자 라벨 | 코드 블록과 굵게만 |
| 기본 온도 | 1.0 | 0.7 |
| 대화 제목 | 캐릭터 이름 | 첫 질문에서 자동으로 |

두 모드의 대화 목록은 탭으로 분리됩니다. 섞이지 않습니다.

어시스턴트 프롬프트와 샘플링 값은 **설정** 아래쪽에서 따로 조절합니다.
롤플레이는 문장이 튀어야 재미있어서 온도가 높고, 어시스턴트는 정확해야 해서 낮게 잡아 뒀습니다.

### 생각

입력창 아래 **생각** 을 켜면 모델이 답하기 전에 먼저 따져 봅니다.
그 과정은 **접힌 채로** 나오므로 답변을 가리지 않습니다. 눌러서 펼쳐 보면 됩니다.

| 엔진 | 켤 때 | 끌 때 |
|---|---|---|
| Gemini 3 계열 | `includeThoughts` 만 켜고 깊이는 모델 기본값 | `thinkingLevel: "low"` |
| Gemini 2.5 계열 | 위와 같음 | `thinkingBudget: 0` |
| Gemini API 의 Gemma | 지원하지 않음 — 거부되면 알아서 빼고 재시도 | 〃 |
| Anthropic | 확장 사고(`thinking`). 세대마다 방식이 달라 거부되면 물러섬 | 보내지 않음 |
| LM Studio (Gemma 4) | 시스템 프롬프트 앞에 `<\|think\|>` | 토큰 제거 |
| OpenAI | 추론 모델은 내부적으로 생각하지만 그 내용을 내려보내지 않음 | — |

**켤 때 깊이를 따로 지정하지 않습니다.** `thinkingLevel: "high"` 로 올리면 첫 글자가 나오기까지
한참 걸립니다. 모델 기본값(대개 동적 사고)이 무난합니다.

**끄기가 완전한 끄기가 아닌 모델이 있습니다.** 문서상 Gemini 3.1 Pro, 3 Flash, Flash-Lite 는
사고를 완전히 끌 수 없고 최소화만 됩니다. 토글을 꺼도 약간의 사고 토큰이 청구될 수 있습니다.

**응답 최대 길이에 사고 토큰이 함께 셉니다.** 짧게 잡아 두면 모델이 생각하다가 한도에 걸려
본문이 비거나 잘린 채 끝납니다. 그래서 사고를 켤 때는 앱이 바닥을 올려 줍니다 —
Gemini 는 최소 4096, Anthropic 은 사고 예산 2048 에 답변용 2048 을 더해 잡습니다.
설정값이 그보다 크면 그대로 씁니다.

사고 내용은 **다음 턴에 함께 보내지 않습니다.** 대화 기록과 분리해 저장하므로 맥락을 잡아먹지 않습니다.

사고 토큰도 요금에 포함됩니다. 느려지기도 하니 복잡한 질문에만 켜는 쪽이 낫습니다.

### 웹 검색

입력창 아래 **웹 검색** 을 켜면 모델이 필요할 때 인터넷을 찾아보고, 답변 아래에 출처를 접어서 붙입니다.
펼치면 사이트 이름이 나오고, 눌러서 바로 열 수 있습니다.

Gemini 의 근거 링크는 `vertexaisearch.cloud.google.com/grounding-api-redirect/…` 형태의 리다이렉트라
주소 자체는 길고 읽히지 않습니다. 그래서 주소 대신 사이트 이름을 보여 주고 주소는 링크로만 답니다.
출처는 본문과 따로 저장되므로 다음 턴에 함께 보내지지 않습니다.

| 엔진 | 지원 | 쓰는 도구 |
|---|---|---|
| Google Gemini | ○ | Grounding with Google Search |
| Anthropic | ○ | web_search 서버 도구 |
| OpenAI | △ | `gpt-5-search-api` 전용. 일반 모델은 토글이 잠깁니다 |
| LM Studio · 로컬 | ✕ | 로컬 모델에는 검색 도구가 없습니다 |

검색이 안 되는 조합을 고르면 토글이 회색으로 잠기고, 왜 안 되는지 툴팁에 나옵니다.

OpenAI 는 Chat Completions 에서 **전용 검색 모델만** 검색합니다. 문서 기준 `gpt-5-search-api` 가 현행이고,
`gpt-4o-search-preview` 계열은 2026년 7월에 종료됐습니다. 그래서 모델 이름에 search 가 없으면 토글이 잠깁니다.

Gemini 엔진에 Gemma 모델을 물려 둔 경우도 검색이 안 됩니다. 시도하면 무엇이 문제인지 알려 줍니다.

검색은 토큰을 많이 먹습니다. 찾아온 본문이 통째로 프롬프트에 들어가기 때문에,
최신 정보가 필요한 질문에만 켜는 쪽이 낫습니다. 설정은 계속 유지되니 쓰고 나면 끄세요.

### 마크다운

어시스턴트 모드는 마크다운을 렌더링합니다.

| 표기 | 결과 |
|---|---|
| `# 제목` ~ `###### 제목` | 제목 (여섯 단계) |
| `- 항목` / `1. 항목` | 목록. 들여쓰면 중첩됩니다 |
| `**굵게**` `*기울임*` `~~취소~~` | 강조 |
| `` `코드` `` / ``` 블록 ``` | 코드 |
| `> 인용` | 인용문 |
| `---` | 구분선 |
| `[글자](주소)` | 링크 |
| `\| 표 \|` | 표 |

모델이 `$\rightarrow$` 같은 수식 표기를 섞어 쓰는 경우가 있어, 흔한 것은 기호로 바꿉니다.
바꾸지 못한 명령이 남아 있으면 원문 그대로 둡니다. 어설프게 지우면 뜻이 달라지기 때문입니다.

외부 라이브러리를 쓰지 않고 직접 구현했습니다. 로컬 모델만 켜고 오프라인으로 쓰는 경우가 많은데,
CDN 에 기대면 그럴 때 글이 통째로 날것으로 보입니다.

롤플레이 모드에는 적용되지 않습니다. 거기서는 별표가 행동 묘사이므로 기울임으로 바뀌면 안 됩니다.

---

## 11. 개발자 설정

**설정 → 개발자 설정** 에서 엽니다. 예전에는 파일을 직접 고쳐야 했던 것들입니다.

### 메시지 표시 방식

모델이 내놓은 글을 화면에서 어떻게 꾸밀지 네 가지를 켜고 끕니다.

| 토글 | 하는 일 |
|---|---|
| `*별표*` 를 묘사체로 | 별표 안을 흐린 기울임으로 |
| `(괄호)` 를 묘사체로 | 괄호 안을 흐린 기울임으로 |
| `이름:` 을 화자 라벨로 | 줄 맨 앞의 이름을 강조색으로 |
| `"따옴표"` 안을 대사로 | 따옴표 안을 밝게 |

체크를 바꾸면 지금 열린 대화에 **즉시** 반영되므로 결과를 보면서 고를 수 있습니다.
저장된 원문은 건드리지 않습니다. 표시만 바뀝니다.

괄호를 자주 쓰는 문체인데 자꾸 기울어져 거슬린다면 괄호 토글만 끄면 됩니다.

### 한국어 조사 자동 교정

`{{char}}가` 를 이름의 받침에 맞춰 "유하린이 / 루나가" 로 고치는 기능입니다.
영문 이름이 많거나 교정이 오히려 어색하면 끄세요. 끄면 프롬프트에 쓴 조사가 그대로 나갑니다.

### 테마

배경·패널·선·글자·흐린 글자·강조, 여섯 색과 글꼴·글자 크기를 바꿉니다.
색을 고르는 즉시 화면에 반영됩니다. **기본 테마로** 를 누르면 되돌아갑니다.

패널 색에서 보조 패널 색을, 강조색에서 테두리용 색을 자동으로 계산하므로 여섯 개만 정하면 됩니다.

글꼴은 CSS `font-family` 값을 그대로 씁니다. PC 에 설치된 글꼴이나 이미 불러온 웹폰트만 적용됩니다.
예: `'Noto Sans KR', sans-serif`

### 엔진 추가

Ollama, vLLM, llama.cpp, OpenRouter 처럼 OpenAI 호환 서버를 붙일 수 있습니다.
이름·형식·주소·키를 넣고 **엔진 추가** 를 누른 뒤 시트를 저장하면, 설정의 엔진 목록에 나타납니다.

| 예 | 형식 | 주소 |
|---|---|---|
| Ollama | OpenAI 호환 | `http://localhost:11434/v1` |
| llama.cpp | OpenAI 호환 | `http://localhost:8080/v1` |
| OpenRouter | OpenAI 호환 | `https://openrouter.ai/api/v1` |

주소가 `localhost` 나 사설 IP 면 API 키를 비워 둬도 됩니다.
같은 이유로 로컬 주소의 커스텀 엔진에서는 성인 틀도 쓸 수 있습니다. 외부 주소면 막힙니다.

내장 엔진 넷은 지울 수 없습니다. 직접 추가한 것만 삭제 버튼이 붙습니다.

### 감춰 둔 모델

목록에 있길래 골랐는데 막상 쓰려니 404 가 나는 모델이 있습니다. Gemini 가 특히 그런데,
구글이 2.5 계열을 "예전부터 써 온 계정에만" 열어 둔 탓입니다. 같은 모델이 어떤 계정에서는 되고
어떤 계정에서는 안 되니, 이름만 보고 미리 거를 방법이 없습니다.

그래서 앱은 **한 번 거부당한 모델을 기억해 목록에서 뺍니다.** 오류 메시지에 대체 모델이 적혀 있으면
그것도 같이 알려줍니다.

```
'gemini-2.5-flash-lite' 은 이 계정에서 쓸 수 없는 모델입니다. 목록에서 감췄습니다.
API 가 'gemini-3.5-flash-lite' 를 대신 권합니다.
설정에서 모델을 다시 고르세요.
```

여기 목록에 엔진별로 쌓입니다. 계정 등급이 바뀌었거나 구글이 다시 열어 줬다면 **삭제** 를 눌러
되돌린 뒤 시도해 보세요.

### 진단

**시스템 프롬프트 열기** 를 누르면 별도 창이 뜹니다. 이 대화에서 실제로 모델에 보내는 전문이 그대로 나옵니다.

창 위쪽에 대화 이름, 쓰고 있는 틀, 엔진이 표시되고, 오른쪽에 글자 수와 어림 토큰 수,
같이 보내는 최근 대화 턴 수가 나옵니다. 아래 **복사** 로 전문을 가져갈 수 있습니다.

캐릭터 설정이 제대로 들어갔는지, 비어 있는 항목이 어떻게 빠졌는지, 조사 교정이 어떻게 적용됐는지를
한눈에 확인할 수 있습니다. 캐릭터가 설정을 무시하는 것 같을 때 여기부터 보세요.

토큰 수는 글자 수로 어림한 값입니다. 실제 토큰은 모델의 토크나이저에 따라 달라집니다.
컨텍스트 길이를 잡을 때 대략의 감을 잡는 용도로만 쓰세요.

---

## 12. API 엔진으로 바꾸기

로컬 모델이 막힐 때, 더 좋은 문장이 필요할 때, PC 가 바쁠 때 갈아탑니다.
**설정에서 엔진만 바꾸면 다음 응답부터 적용됩니다.** 대화를 새로 시작할 필요가 없습니다.

### 키 발급

| 엔진 | 키 받는 곳 | 키 생김새 |
|---|---|---|
| Google Gemini | Google AI Studio 의 API 키 메뉴 | `AIza…` |
| OpenAI | OpenAI 플랫폼의 API keys 메뉴 | `sk-…` |
| Anthropic | Anthropic 콘솔의 API Keys 메뉴 | `sk-ant-…` |

키는 발급 직후에만 전체가 보입니다. 그 자리에서 복사해 두세요.

### 앱에 넣기

설정에서:

1. 사용할 엔진을 고릅니다
2. 서버 주소는 기본값 그대로 둡니다

   | 엔진 | 주소 |
   |---|---|
   | OpenAI | `https://api.openai.com/v1` |
   | Anthropic | `https://api.anthropic.com/v1` |
   | Gemini | `https://generativelanguage.googleapis.com/v1beta` |
   | Ollama | `https://ollama.com/v1` (클라우드) · `http://localhost:11434/v1` (내 PC 의 Ollama) |

3. API 키를 붙여 넣습니다. 입력한 키는 점으로 가려집니다. 제대로 들어갔는지 확인하려면 옆의 **보기** 를 누르세요. 설정을 다시 열거나 엔진을 바꾸면 자동으로 다시 가려집니다
4. **불러오기** 를 눌러 모델 목록을 받습니다. 목록이 바로 펼쳐지고, 글자를 치면 걸러집니다.
   ↑↓ 로 옮기고 Enter 로 고릅니다. 목록에 없는 이름을 직접 적어도 됩니다
5. **저장**

엔진마다 주소·키·모델이 **따로 저장됩니다.** 한 번 넣어 두면 이후에는 엔진 선택만 바꾸면 됩니다.

> 모델 이름은 계속 바뀝니다. 기억해 둔 이름을 직접 타이핑하기보다 **불러오기** 목록에서 고르는 쪽이 안전합니다.

**Ollama** 는 클라우드와 로컬 둘 다 이 칸 하나로 씁니다.

- **클라우드**: [ollama.com/settings/keys](https://ollama.com/settings/keys) 에서 API 키를 만들어 붙여 넣고 **불러오기**. 목록에 나오는 이름(예: `gemma4:31b`)을 그대로 고르세요. 모델을 받을 필요가 없습니다
- **로컬**: 주소를 `http://localhost:11434/v1` 로 바꾸면 키 없이 됩니다. `ollama pull` 로 받아 둔 모델이 목록에 나옵니다
- 클라우드는 외부 API 라 **성인 모드 대화는 보낼 수 없습니다.** 로컬 주소일 때만 됩니다
- 컨텍스트 길이 기본값은 32768 입니다. 클라우드 요금제 사용량을 아끼려는 값이라, 필요하면 늘리세요

Gemini 는 목록을 두 가지로 거릅니다. 임베딩·이미지 생성 전용 모델은 대화에 쓸 수 없어 빠지고,
목록이 여러 쪽으로 나뉘어 오면 끝까지 따라가 모읍니다.
**불러오기를 눌렀는데 0개가 나오면** 키 자체는 유효하지만 Generative Language API 가
그 키/프로젝트에서 켜져 있지 않은 경우가 많습니다. Google AI Studio 에서 키 상태를 확인하세요.

### 엔진별로 알아둘 것

**비용** — API 는 주고받은 토큰만큼 과금됩니다. 롤플레이는 매 턴마다 시스템 프롬프트와 최근 대화 전체를
다시 보내기 때문에, 대화가 길어질수록 턴당 비용이 올라갑니다. 엔진의 *컨텍스트 길이* 나 *최대 메시지 수* 를 줄이면 그만큼 내려갑니다.
로컬 모델에서 20으로 쓰던 값을 API 에서는 12~16 정도로 낮춰 쓰는 것도 방법입니다.

**콘텐츠 정책** — API 엔진에는 각 회사의 정책이 적용됩니다. 로컬 모델에서 잘 되던 장면이 거부될 수 있습니다.
거부 응답이 오면 화면에 그대로 표시됩니다. 엔진 문제지 앱 오류가 아닙니다.

**엔진마다 다른 파라미터 규칙** — 앱이 알아서 맞춰 보내므로 따로 설정할 건 없습니다.

| 항목 | 규칙 |
|---|---|
| top_k | LM Studio · Anthropic · Gemini 만 받습니다. OpenAI 는 400 을 내므로 빼고 보냅니다 |
| 온도 상한 | Anthropic 은 1, 나머지는 2. 1 을 넘겨 두면 Anthropic 에만 1 로 잘라 보냅니다 |
| 응답 최대 길이 | OpenAI 는 `max_tokens` 가 폐기돼 `max_completion_tokens` 로 보냅니다. 로컬 서버는 아직 `max_tokens` 를 씁니다 |
| 추론 모델 | o3·gpt-5 계열은 온도와 top_p 를 거부합니다. 거부당하면 그 값을 빼고 한 번 더 보냅니다 |

마지막 줄이 중요합니다. 모델 이름만으로는 추론 모델인지 완벽히 가릴 수 없어서,
400 이 오면 오류 문구를 읽고 문제가 된 파라미터를 빼거나 이름을 바꿔 자동으로 재시도합니다.
새 모델이 나와도 코드를 고칠 필요가 없습니다.

**응답 최대 길이** — API 모델은 2048 을 넉넉히 소화합니다. 소설가 모드라면 3000~4000 까지 올려도 됩니다.

### 엔진 섞어 쓰기

같은 대화에서 턴마다 엔진을 바꿔도 됩니다. 대화 기록은 엔진과 무관하게 하나로 쌓입니다.

- 장면 묘사나 분위기 잡는 턴 → 성능 좋은 API
- 가볍게 티키타카하는 턴 → 로컬 모델

각 응답에는 어떤 엔진·모델로 생성했는지가 함께 저장됩니다.

---

## 12-1. 장면 그리기 (ComfyUI)

1. ComfyUI 를 켭니다 (기본 주소 `http://127.0.0.1:8188`)
2. 앱 설정 → **이미지 설정** → *장면 그리기 켜기*, 주소 입력 → **연결 확인** → 체크포인트 선택 → 저장
3. 캐릭터 창의 **외형 태그** 칸에 머리·눈·옷을 영어 태그로 적습니다 (예: `1girl, adult, long black hair, red eyes, gray hoodie`)
4. 답변에 마우스를 올리고 **🎨 그리기**

| 증상 | 확인할 것 |
|---|---|
| 연결하지 못했습니다 | ComfyUI 가 켜져 있는지, Docker 라면 주소가 PC IP 인지와 `LOCAL_ENGINE_HOSTS` |
| 노드 오류 | 올린 워크플로의 노드·모델 이름. 오류 문구에 노드 번호가 나옵니다 |
| 그리지 않았습니다 (미성년…) | 외형 태그나 장면에 미성년으로 읽히는 표현이 있습니다. 이 차단은 끌 수 없습니다 |
| 답변이 느려짐 | 같은 GPU 라면 *그린 뒤 ComfyUI 메모리 비우기* 를 켜고, LM Studio 에서 MoE 전문가를 CPU 로 |

## 13. 데이터와 백업

데이터는 항목별 파일로 나뉘어 있습니다. 대화 하나를 찾으려고 전체를 뒤질 일이 없고,
편집기로 열어 직접 고치기도 쉽습니다.

```
data/
  settings.json          엔진·키·대화 모드·테마
  characters/<id>.json   캐릭터 하나가 파일 하나
  personas/<id>.json     페르소나
  chats/<id>.json        대화 하나가 파일 하나 (메시지 포함)
```

- **백업** — `data` 폴더를 통째로 복사하면 끝입니다
- **옮기기** — 새 PC 에 앱을 설치하고 `data` 폴더를 덮어쓰면 그대로 이어집니다
- **대화 하나만 주고받기** — `chats/<id>.json` 파일 하나만 옮기면 됩니다
- **설정의 백업 내려받기** — 전부를 한 파일로 묶되 API 키는 뺀 사본을 받습니다
- **설정의 백업 불러오기** — 내려받은 파일을 지금 데이터에 **합칩니다**. 지우거나 덮지 않습니다
  - 이미 있는 항목(같은 id, 또는 내용이 똑같은 캐릭터·페르소나)은 건너뛰므로 여러 번 불러와도 겹치지 않습니다
  - 새로 설치한 앱에 불러와도 기본 캐릭터가 두 벌 생기지 않고, 가져온 대화는 원래 캐릭터에 그대로 이어집니다
  - 샘플링 값·어시스턴트 프롬프트·테마·표기법은 물어본 뒤 원할 때만 덮어씁니다. 엔진 주소와 API 키는 바뀌지 않습니다

예전 버전에서 쓰던 `db.json` 이 있으면 서버를 처음 올릴 때 자동으로 쪼개집니다.
원본은 지우지 않고 `db.json.migrated` 로 이름만 바꿔 두니, 문제가 생기면 되돌릴 수 있습니다.

> `settings.json` 에는 API 키가 평문으로 들어 있습니다. 그대로 공유하거나 깃 저장소에 올리지 마세요.
> (`.gitignore` 에 `data/` 가 이미 들어 있습니다.)

---

## 14. 문제 해결

### 모델 목록을 불러오지 못합니다

1. LM Studio 의 Developer 탭에서 서버가 켜져 있는지
2. 포트가 1234 인지
3. 브라우저에서 http://localhost:1234/v1/models 가 열리는지

셋 다 맞는데 안 되면 앱 설정의 서버 주소 끝에 `/v1` 이 붙어 있는지 확인하세요.

### "생성 실패" 나 빨간 오류 줄이 뜹니다

오류 문구가 그대로 표시됩니다.

| 문구 | 원인 |
|---|---|
| `fetch failed` | LM Studio 서버가 꺼져 있거나 주소가 틀림 |
| `401` / `invalid api key` | API 키가 틀렸거나 만료 |
| `429` | 요청 한도 초과. 잠시 뒤 다시 |
| `모델을 먼저 지정하세요` | 설정에서 모델을 안 골랐음 |
| `이 계정에서 쓸 수 없는 모델` | 목록에는 있지만 이 계정에 안 열린 모델. 자동으로 감춰집니다 |
| `400` + `top_k` | OpenAI 에 top_k 가 간 경우 (정상 동작에서는 발생하지 않습니다) |

### 응답에 `<|channel>thought` 같은 태그가 보입니다

Gemma 4 는 사고 모드를 꺼도 빈 사고 블록을 출력합니다.
앱이 이를 걸러내지만, 새로운 형태의 태그라면 놓칠 수 있습니다.
`src/sanitize.js` 의 `OPEN` / `CLOSE` 배열에 해당 태그를 추가하면 됩니다.

### 캐릭터가 초반 설정을 잊습니다

컨텍스트가 넘쳐 앞부분이 잘린 것입니다.

- 엔진의 *컨텍스트 길이* 를 LM Studio 설정과 같게 맞추세요 (게이지가 한도를 넘지 않게)
- 잊으면 안 되는 설정은 캐릭터의 **추가 설정** 칸에 넣으세요. 이건 매번 다시 보내집니다

### 응답이 문장 중간에 끊깁니다

*응답 최대 길이* 가 모자란 것입니다. 2048 → 3072 로 올려 보세요.

### 같은 말이 끝없이 되풀이됩니다

"나찰의 나찰의 나찰의…" 처럼 한 조각이 반복되는 것은 모델이 루프에 빠진 상태입니다.
앱이 이를 감지해 생성을 멈추고 아래 문구를 붙입니다.

```
(같은 말이 되풀이되어 생성을 멈췄습니다. 재전송을 누르거나, 설정에서 반복 억제 값을 올려 보세요.)
```

멈추기만 할 뿐 원인은 따로 잡아야 합니다. 위에서부터 하나씩 해 보세요.

**1. 반복 억제를 올립니다** — 설정의 *반복 억제* 를 1.1 에서 1.15~1.2 로.
로컬 엔진에만 전달되는 값입니다. 너무 올리면(1.3 이상) 문장이 어색해지니 조금씩 올리세요.

**2. 재전송을 눌러 봅니다** — 온도 1.0 이면 매번 다르게 나옵니다.
특정 대목에서만 반복된다면 그 직전 메시지를 **수정** 으로 손봐 주세요. 이상한 맥락이 루프를 부르는 경우가 많습니다.

**3. 컨텍스트가 꽉 찼는지 봅니다** — 앞부분이 잘려 나가면 맥락이 어그러져 루프가 잦아집니다.
입력창 아래 게이지가 빨갛다면 이 경우입니다. 설정의 *컨텍스트 길이* 가 LM Studio 의 Context Length 와 같은지 확인하고, *응답 최대 길이* 를 줄이거나 컨텍스트 길이를 늘리세요.
LM Studio 로그의 `n_tokens` 값이 설정한 컨텍스트에 가깝다면 이 경우입니다.

**4. 채팅 템플릿을 확인합니다** — LM Studio 모델 설정의 *Chat Template* 이 모델과 맞지 않으면
출력이 통째로 무너집니다. 자동으로 잡힌 값이 gemma 계열인지 보세요.

**5. KV 캐시 양자화를 하나씩 꺼 봅니다** — 컨텍스트를 늘려도 계속 난다면 여기가 가장 유력합니다.

Gemma 4 는 슬라이딩 윈도우와 글로벌 어텐션을 번갈아 쓰고, 글로벌 레이어는 K 와 V 를 합쳐서 다룹니다.
이런 구조에서 캐시를 8비트로 줄이면 긴 맥락에서 어텐션이 무너져, 방금 쓴 토큰만 붙들고 되풀이하게 됩니다.
LM Studio 화면에도 "일부 모델에서 문제를 일으킬 수 있다" 고 적혀 있는 실험적 기능입니다.

| 순서 | 끌 것 | 비고 |
|---|---|---|
| 1 | V Cache Quantization | V 쪽이 품질 저하가 큽니다. 여기서 멈추는 경우가 많습니다 |
| 2 | K Cache Quantization | 위에서 안 되면 |
| 3 | Unified KV Cache | 위에서 안 되면. 실험적 기능입니다 |

한 번에 다 끄지 말고 하나씩 끄면서 확인해야 원인이 특정됩니다.
끌 때마다 VRAM 이 늘어나므로 *Force MoE weights onto CPU* 를 2~3칸씩 올려 맞추세요.

**6. 모델을 바꿉니다** — heretic 같은 ablation 계열은 원본보다 불안정합니다.
양자화를 한 단계 올리거나(Q4_K_S → Q4_K_M) 다른 모델을 써 보세요.

### 너무 느립니다

1. LM Studio 의 *Estimated Memory Usage* 에서 GPU 수치를 봅니다
2. 15GB 를 크게 밑돌면 **Force MoE weights onto CPU** 를 한 칸 내립니다
3. 컨텍스트 길이가 32768 을 넘지 않는지 확인합니다
4. Max Concurrent Predictions 가 1 인지 확인합니다

### 모델이 아예 로드되지 않습니다 (RTX 5080)

5080 은 Blackwell 세대라 구버전 CUDA 런타임에서 로드가 실패하거나 조용히 CPU 로 떨어집니다.
LM Studio 를 최신으로 올리고, Runtime 탭에서 CUDA 런타임도 갱신하세요.

### 성인 대화에서 "로컬 엔진 전용입니다" 가 뜹니다

의도된 동작입니다. 설정에서 엔진을 LM Studio 로 바꾸거나, 대화 상단 선택기에서 일반 모드를 고르세요.

### 화면이 하얗게 뜹니다

브라우저 개발자 도구(F12) 의 Console 탭에 오류가 찍힙니다.
`Failed to load module` 이면 `public/js` 폴더의 파일이 빠진 것이니 압축을 다시 풀어 보세요.

---

## 15. 더 손보고 싶다면

앱에서 바로 되는 건 여기까지입니다.

| 하고 싶은 것 | 어디서 |
|---|---|
| 엔진 추가 (Ollama, vLLM 등) | 개발자 설정 → 엔진 추가 |
| 메시지 표시 방식 바꾸기 | 개발자 설정 → 메시지 표시 방식 |
| 색과 글꼴 | 개발자 설정 → 테마 |
| 조사 교정 끄기 | 개발자 설정 → 프롬프트 |
| 대화 모드 새로 만들기 | 설정 → 대화 모드 |

**코드를 직접 고쳐야 하는 것**은 이 문서의 범위 밖입니다. 프로젝트 구조, 각 파일의 역할,
엔진 어댑터를 새로 만드는 법, 반복 감지·조사 교정 알고리즘, 로컬 테스트 방법 같은 내용은
같은 폴더의 **`DEVELOPER.md`** 에 정리해 두었습니다.
